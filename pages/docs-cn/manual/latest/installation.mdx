---
title: "Installation"
description: "ReScript installation and setup instructions"
canonical: "/docs-cn/manual/latest/installation"
---

# Installation

## Prerequisites

- [Node.js](https://nodejs.org/) version >= 10
- [npm](https://docs.npmjs.com/cli/) (which comes with Node.js) or [Yarn](https://yarnpkg.com/)

- [Node.js](https://nodejs.org/) 版本 >= 10
- [npm](https://docs.npmjs.com/cli/) (包括Node.js)或[Yarn](https://yarnpkg.com/)


## New Project

```sh
git clone https://github.com/rescript-lang/rescript-project-template
cd rescript-project-template
npm install
npm run build
node src/Demo.bs.js
```

That compiles your ReScript into JavaScript, then uses Node.js to run said JavaScript. **We recommend you use our unique workflow of keeping a tab open for the generated `.bs.js` file**, so that you can learn how ReScript transforms into JavaScript. Not many languages output clean JavaScript code you can inspect and learn from!

这将编译你的ReScript到JavaScript, 然后用Node.js运行JavaScript. **我们推荐在你使用我们独特的工作流: 为生成的`.bs.bs`文件打开一个tab, 从而你可以学习ReScript是如何变换到JavaScript的. 没有多少语言能生成简洁的JavaScript代码以便人们学习.

During development, instead of running `npm run build` each time to compile, use `npm run start` to start a watcher that recompiles automatically after file changes.

在开发过程中不要每次都运行 `npm run build`来编译, 而是使用`npm run start`来启动一个在文件更改后自动重新编译的监视程序.

## Integrate Into an Existing JS Project

If you already have a JavaScript project into which you'd like to add ReScript:

如果你已经有了一个JavaScript项目, 并且想在其中加入ReScript.

- Install ReScript locally as a [devDependency](https://docs.npmjs.com/specifying-dependencies-and-devdependencies-in-a-package-json-file):
  ```sh
  npm install rescript --save-dev
  ```
- Create a ReScript build configuration at the root:
  ```json
  {
    "name": "your-project-name",
    "sources": [
      {
        "dir": "src", // update this to wherever you're putting ReScript files
        "subdirs": true
      }
    ],
    "package-specs": [
      {
        "module": "es6",
        "in-source": true
      }
    ],
    "suffix": ".bs.js",
    "bs-dependencies": []
  }
  ```
  See [构建配置](build-configuration) for more details on `bsconfig.json`.
- Add convenience `npm` scripts to `package.json`:
  ```json
  "scripts": {
    "res:build": "rescript",
    "res:start": "rescript build -w"
  }
  ```

- 在局部通过[devDependency](https://docs.npmjs.com/specifying-dependencies-and-devdependencies-in-a-package-json-file)安装ReScript:
  ```sh
  npm install rescript --save-dev
  ```
- 在根目录下创建ReScript构建配置
  ```json
  {
    "name": "your-project-name",
    "sources": [
      {
        "dir": "src", // update this to wherever you're putting ReScript files
        "subdirs": true
      }
    ],
    "package-specs": [
      {
        "module": "es6",
        "in-source": true
      }
    ],
    "suffix": ".bs.js",
    "bs-dependencies": []
  }
  ```
  关于`bsconfig.json`的更多细节, 请查看[构建配置](build-configuration).
- 为了方便, 可以在`package.json`中加上`npm`脚本:
  ```json
  "scripts": {
    "res:build": "rescript",
    "res:start": "rescript build -w"
  }
  ```


Since ReScript compiles to clean readable JS files, the rest of your existing toolchain (e.g. Babel and Webpack) should just work!

因为ReScript文件可以编译到简洁可读的JS文件, 其他的已有工具链应该是直接可用的.

Helpful guides:

- [Converting from JS](converting-from-js).
- [Shared Data Types](shared-data-types).
- [Import from/Export to JS](import-from-export-to-js).

其他有用的指南:
- [从JS迁移到ReScript](converting-from-js).
- [共享数据类型](shared-data-types).
- [Import from/Export to JS](import-from-export-to-js).


### Integrate with a ReactJS Project

To start a [rescript-react](/docs/react/latest/introduction) app, or to integrate ReScript into an existing ReactJS app, follow the instructions [here](/docs/react/latest/installation).

如果需要创建一个[rescript-react](/docs/react/latest/introduction) app或在现有的ReactJS app中整合ReScript, 请跟随这里[这里](/docs/react/latest/installation)的指南
